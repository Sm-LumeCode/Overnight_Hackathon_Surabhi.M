{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    // FIX: Add 'baseUrl' to define the root for path mapping.
    "baseUrl": ".",
    "paths": {
      "@/*": [
        "./*"
      ]
    }
  },
  "include": [
    "**/*.ts",
    "**/*.tsx",
    ".expo/types/**/*.ts",
    "expo-env.d.ts"
, "scripts/testGemini.js"  ]
}
